@model MyWebApplication.Models.ViewModel.CategoryVM
<form method="post" asp-action="CreateUpdate">
    <div class="border p-3 mt-4">
        <div class="row pb-3">
            <h2 class="text-primary">@((Model.category==null||Model.category.Id==0)?"Create":"Update") Category</h2>
            <hr />
        </div>
        <div asp-validation-summary="All"></div>
        <div class="row mb-3">
            <label asp-for="category.Name"></label>
            <input asp-for="category.Name" class="form-control" />
            <span asp-validation-for="category.Name" class="text-danger"></span>
            <input type="hidden" asp-for="category.Id"  />

        </div>
        <div class="row mb-3">
            <label asp-for="category.DisplayOrder"></label>
            <input asp-for="category.DisplayOrder" class="form-control" />
            <span asp-validation-for="category.DisplayOrder" class="text-danger"></span>
        </div>
        @if (Model.category == null || Model.category.Id == 0)
        {
            <button class="btn btn-primary" asp-controller="category" asp-action="CreateUpdate" type="submit">Create</button>
        }
        else
        {
            <button class="btn btn-primary" asp-controller="category" asp-action="CreateUpdate" type="submit">Update</button>
        }

    </div>
    <a asp-action="Index" asp-controller="category">Back to List</a>
</form>
@section scripts{
    @{
         <partial name="_ValidationScriptsPartial"/>
    }
}

